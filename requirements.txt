# requirements.txt
flask==0.12  # old version, has known vulnerabilities
requests==2.19.1  # old version, may have vulnerabilities
urllib3>=2.6.0 # not directly required, pinned by Snyk to avoid a vulnerability
